spring:
  application:
    name: callback-mq-consumer
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
management:
  health:
    db:
      enabled: false
server:
  tomcat:
    mbeanregistry:
      enabled: true