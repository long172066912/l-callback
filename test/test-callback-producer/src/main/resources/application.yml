spring:
  application:
    name: callback-producer
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
management:
  health:
    db:
      enabled: false
server:
  tomcat:
    mbeanregistry:
      enabled: true